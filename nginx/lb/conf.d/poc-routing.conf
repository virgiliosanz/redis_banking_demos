upstream fe_live {
    zone fe_live 64k;
    server fe-live:9000 resolve;
    keepalive 16;
}

upstream fe_archive {
    zone fe_archive 64k;
    server fe-archive:9000 resolve;
    keepalive 16;
}

upstream be_admin {
    zone be_admin 64k;
    server be-admin:9000 resolve;
    keepalive 16;
}

map $host $host_context {
    default live;
    archive.nuevecuatrouno.test archive;
}

map $uri $path_context {
    default live;
    ~^/(2015|2016|2017|2018|2019|2020|2021|2022|2023)(/|$) archive;
}

map $uri $is_admin_path {
    default 0;
    ~^/wp-admin(/|$) 1;
    /wp-login.php 1;
    /wp-admin/admin-ajax.php 1;
    ~^/wp-json(/|$) 1;
}

map "$host|$is_admin_path|$uri" $archive_host_public_block {
    default 0;
    ~^archive\.nuevecuatrouno\.test\|0\|/(?!healthz$|ping$|status$).+ 1;
}

map "$is_admin_path|$host_context|$path_context" $site_context {
    default live;
    "1|live|live" live;
    "1|live|archive" live;
    "1|archive|live" archive;
    "1|archive|archive" archive;
    "0|live|live" live;
    "0|live|archive" archive;
    "0|archive|live" archive;
    "0|archive|archive" archive;
}

map "$is_admin_path|$site_context" $php_upstream {
    default fe_live;
    "0|live" fe_live;
    "0|archive" fe_archive;
    "1|live" be_admin;
    "1|archive" be_admin;
}

map "$is_admin_path|$site_context" $site_docroot {
    default /var/www/html/live;
    "0|live" /var/www/html/live;
    "0|archive" /var/www/html/archive;
    "1|live" /var/www/html/admin-live;
    "1|archive" /var/www/html/admin-archive;
}

log_format poc_main
    '$remote_addr - $remote_user [$time_local] '
    '"$request" $status $body_bytes_sent '
    'request_id="$request_id" host="$host" uri="$uri" '
    'request_time="$request_time" upstream_response_time="$upstream_response_time" '
    'php_upstream="$php_upstream" site_context="$site_context" '
    'referer="$http_referer" user_agent="$http_user_agent"';

server {
    listen 80 default_server;
    server_name _;

    access_log /var/log/nginx/access.log poc_main;
    error_log /var/log/nginx/error.log warn;
    client_max_body_size 32m;

    add_header X-Frame-Options SAMEORIGIN always;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;

    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }

    if ($archive_host_public_block) {
        return 302 http://nuevecuatrouno.test$request_uri;
    }

    location = /xmlrpc.php {
        access_log off;
        return 403;
    }

    location = /wp-config.php {
        access_log off;
        return 404;
    }

    location ~ /\.(?!well-known).* {
        access_log off;
        deny all;
    }

    location ~* \.(bak|conf|dist|fla|inc|ini|log|psd|sh|sql|swp)$ {
        access_log off;
        deny all;
    }

    location ~* ^/(wp-content/uploads|wp-content/themes|wp-content/plugins|wp-includes)/.+\.(avif|css|eot|gif|ico|jpeg|jpg|js|json|map|png|svg|txt|webp|woff|woff2)$ {
        root $site_docroot;
        access_log off;
        expires 1h;
        try_files $uri =404;
    }

    location / {
        try_files $site_docroot$uri $site_docroot$uri/ @wordpress_front;
    }

    location @wordpress_front {
        include /etc/nginx/fastcgi_params;
        fastcgi_keep_conn on;
        fastcgi_param SCRIPT_FILENAME $site_docroot/index.php;
        fastcgi_param DOCUMENT_ROOT $site_docroot;
        fastcgi_param SCRIPT_NAME /index.php;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param REQUEST_URI $request_uri;
        fastcgi_param SERVER_NAME $host;
        fastcgi_param REQUEST_SCHEME $scheme;
        fastcgi_param HTTPS $https if_not_empty;
        fastcgi_pass $php_upstream;
    }

    location ~ \.php$ {
        set $php_script $site_docroot$fastcgi_script_name;

        if (!-f $php_script) {
            return 404;
        }

        include /etc/nginx/fastcgi_params;
        fastcgi_keep_conn on;
        fastcgi_param SCRIPT_FILENAME $php_script;
        fastcgi_param DOCUMENT_ROOT $site_docroot;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param REQUEST_URI $request_uri;
        fastcgi_param SERVER_NAME $host;
        fastcgi_param REQUEST_SCHEME $scheme;
        fastcgi_param HTTPS $https if_not_empty;
        fastcgi_pass $php_upstream;
    }
}
